R14 experiment: isolate borrowed JSON object-template construction directly on main, removing the whole-template and planned-array copies inside the existing GC suppression scope. Fresh mutable outputs, cache bounds, root registration and collection hooks are preserved. R11/R12 string-length and construction-context changes are excluded. Correctness, machine-code and performance qualification are pending; no release version bump.
