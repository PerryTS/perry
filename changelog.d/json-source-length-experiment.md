R11 experiment, rejected for landing: source UTF-16 length reuse reduces changing-input Unicode JSON.parse CPU by 47.42% and ASCII-string parse CPU by 12.26%, but small-record parsing slows 0.88% in the longer recheck, with all 11 pairs slower. Allocation, copied output storage, GC policy and fallback semantics are unchanged. See benchmarks/json_performance/SOURCE_LENGTH_R11.md for all CPU/RSS rows and qualification limits. No release version bump.
