R15 experiment: keep borrowed JSON template construction inside the original out-of-line reuse function, removing R14's inlined predicate and separate hit helper. This preserves the parse admission call boundary while avoiding whole-template and planned-array copies under the existing GC suppression scope. Mutable output, cache admission and bounds, root registration, collection hooks and allocation policy are unchanged. R11/R12 string-length changes are excluded. Rejected for landing: the 12-case screen and longer recheck retain large-workload CPU slowdowns despite the small-object gains. All 424 timed samples, 70 output checks and correctness evidence are preserved in benchmarks/json_performance/TEMPLATE_CALL_BOUNDARY_R15.md. The branch is an experiment, with no PR or release version bump.
