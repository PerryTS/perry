R15 experiment: keep borrowed JSON template construction inside the original out-of-line reuse function, removing R14's inlined predicate and separate hit helper. This preserves the parse admission call boundary while avoiding whole-template and planned-array copies under the existing GC suppression scope. Mutable output, cache admission and bounds, root registration, collection hooks and allocation policy are unchanged. R11/R12 string-length changes are excluded. Correctness, machine-code and performance qualification are pending; no release version bump.
