R13 experiment: reconstruct cached JSON objects from a borrowed immutable template inside the existing GC suppression scope, avoiding whole-template and planned-array copies. The cache-match predicate is separate from the construction body so misses need not enter its large frame. R12 source-length reuse is retained. Mutable outputs, cache bounds, root registration, collection hooks and allocation policy are unchanged. Correctness, generated-code and performance qualification are pending; no release version bump.
