R13 experiment, rejected for landing: borrowing the JSON object template removes its internal copies and shrinks the hit-helper frame from 912 to 224 bytes. Repeated-input small-record parsing improves 10.47% in the longer recheck, but changing-input parsing remains 0.93% slower in all eleven pairs, with overlapping ranges; the initial 0.82% slowdown had separated ranges. R12 source-length reuse is retained. The combined candidate does not establish the required absence of regressions. The report in `benchmarks/json_performance/BORROWED_TEMPLATE_R13.md` records all CPU/RSS samples, build and correctness evidence, unchanged baseline gaps, and remaining qualification. Parked without a PR or release version bump.
