Cache immutable Perex programs by source identity and canonical flags, with bounded LRU eviction and GC relocation support. Reuse validated program bindings across searches and reduce builtin RegExp test and empty-string replacement overhead while preserving independent `lastIndex`, overrides, and `RegExp.prototype.compile` behavior. This removes repeated compilation of emoji-regex in string-width (#10179).
