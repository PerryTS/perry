Archived R9 experiment: preserve the original compact JSON.stringify admission, then attempt primitive true and numeric zero spacing through a separate bounded-output helper. Zero-spacing calls improve about 9.3× versus main and key-list output improves 1.92%, but ordinary small-record stringify slows 2.82%; this version is rejected for landing. LLVM moves three previously inlined writers into separate calls. The full seven-row CPU/RSS comparison, 196 verified samples, exact build/input provenance, copying-GC checks and baseline correctness limitations are recorded in benchmarks/json_performance/INERT_SPACER_TAIL_R9.md. GC policy and lazy-source admission are unchanged.
