Admit primitive true and either sign of numeric zero to existing bounded JSON.stringify output paths when no replacer is present. Computed spacers benefit as well as literals; boxed Number/String coercions stay observable. The separate lazy-source shortcut retains its existing admission because its raw-output and generic-fallback gaps need independent correction. This R8 experiment is based on fresh main; performance qualification is pending.
