R12 experiment: pass the parser as a compile-time string construction context and move source-length reuse behind the constructor's large-leaf dispatch. This removes R11's separate size check and source argument loading from the per-string parser path. Source-length admission, copied output storage, allocation policy and GC are unchanged. Machine-code and performance qualification are pending; no release version bump.
