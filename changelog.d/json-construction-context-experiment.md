R12 experiment, rejected for landing: compile-time construction context preserves the large-string parse gains and improves 1 MiB array parsing, but small-record repeated-input parsing slows 1.09% in the longer recheck. Changing-input small records remain 0.78% slower with overlapping sample ranges; all 11 pairs are slower in both cases. See benchmarks/json_performance/CONSTRUCTION_CONTEXT_R12.md for all measurements and qualification limits. No release version bump.
