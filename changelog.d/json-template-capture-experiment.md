R16 experiment: build one local JSON object template directly and borrow planned arrays during root-barrier iteration, aiming to remove redundant template-capture copies. Shape lookup still follows value admission, and root barriers precede complete cache publication. Rejected captures leave the prior plan unchanged. Cache-hit construction, cache bounds and admission, rooting and GC policy retain main behavior. Parked before performance timing: machine-code inspection retains both copy calls and the 1376-byte frame, moving the intermediate copy to initialization. Correctness and exact census checks pass; no CPU/RSS claim, PR or release version bump. Evidence: benchmarks/json_performance/TEMPLATE_CAPTURE_R16.md.
