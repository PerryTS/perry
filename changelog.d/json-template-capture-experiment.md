R16 experiment: build one local JSON object template directly and borrow planned arrays during root-barrier iteration, removing redundant template-capture copies. Shape lookup still follows value admission, and root barriers precede complete cache publication. Rejected captures leave the prior plan unchanged. Cache-hit construction, cache bounds and admission, rooting and GC policy retain main behavior. Correctness, machine-code and performance qualification are pending; no release version bump.
