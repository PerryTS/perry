Versioned checked-reader loops now recognize a narrow family of exact additive arrow callbacks and keep admitted array handles in the loop preheader while the callback stays on its non-collecting path. Property-cache misses, dynamic addition, identity or capture-layout mismatches, and local exception scopes fail closed to an exact next-index resume through the ordinary guarded loop, eliminating steady-state callback guards without changing observable fallback behavior.
