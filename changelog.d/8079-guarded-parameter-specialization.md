### Performance

- Recover ordinary typed-parameter optimization in runtime-guarded function clones while preserving the conservative generic fallback for erased or lying TypeScript annotations (#8079).
