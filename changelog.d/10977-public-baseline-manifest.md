Fix public benchmark input hashing to retain the complete root Cargo manifest while normalizing only the value of `[workspace.package].version`. Profiles, dependencies, features, workspace members and edition changes invalidate evidence. Tests exercise Git-discovered fingerprints and the validator in both directions. Regenerate the previously stale public artifact on the quiet Apple M1 mini with pinned Node v22.23.1 and Bun 1.3.14: 50 workloads, 1,443 raw timing samples, all correctness checks passing. Refresh the generated README and suite tables, including losses. Against the regenerated evidence, a release-version bump stays green while profile and dependency mutations fail and their reverts pass.
