Fix inlined named class-expression factories losing their per-evaluation self-binding (#11154). Remap `ClassExprFresh.evaluation_owner` with the same local-ID substitution as its captures and static initializers, so static blocks can read their own class and modify the correct prototype. Add transform regressions for remapped, absent, and outer owners, plus native parity coverage of repeated factory calls and independent prototype writes.
