Restore the pre-release CI gates: eliminate Rust warning failures, make the
structural lint audits reflect realm-local storage and current GC store sites,
tighten raw-handle debt across allocation windows, split the oversized timer
module, and execute the Windows ARM64 smoke artifact through PowerShell's
explicit invocation path.
