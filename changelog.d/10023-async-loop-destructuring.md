Keep destructured loop bindings alive across later awaits. Per-iteration liveness previously examined only the suffix of the immediate declaration block, missing reads outside the synthetic `Try` used for array destructuring. Such bindings now retain their activation storage when references extend outside that block. Includes transform tests and Node parity coverage for conditional awaits, multiple iterations, escaped closures, and awaited defaults.
