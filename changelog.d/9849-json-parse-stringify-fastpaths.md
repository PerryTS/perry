Accelerate JSON parse and stringify with bounded scanners, vectorized Unicode handling, duplicate-key indexing, bounded parse-shape retention, ECMAScript number formatting, inline scalar results, primitive-array/object emission and direct final output for qualifying strings and records. Exact integer-valued doubles below 2^53 use integer formatting in direct-output plans, preserving negative zero and shortest-roundtrip boundaries.

Root parse inputs before pending collection and rederive bytes afterward. Build native tapes through a raw-input API whose byte borrow ends before collection. Honor later-record descriptors in shape templates and array index getters in the generic compact serializer. Root direct-output records before the allocating first prototype lookup and final allocation. GC production policy, thresholds and gc_bump_malloc_trigger remain unchanged from v0.5.1520; GC-directory changes are tests only.

The latest measured source passes 152 JSON runtime tests, 35 compiled Node comparisons and 28 moving-GC runs. Its complete M1 CPU/RSS matrix and 532-trial paired comparison pass quiet gates and external monitoring. Against the preceding direct-record candidate, paired tiny-object/small-record/1 KB object stringify CPU falls 30.20%/21.59%/14.40%.

This remains a development checkpoint: only 11/38 CPU, 58/74 peak RSS and 28/36 retained RSS targets meet the better Node/Bun median. Paired parse regressions include empty/tiny/small/1 KB objects at 3.39%/2.87%/1.79%/4.53% and long-ASCII/Unicode payloads at 5.41%/3.35%; earlier memory and CPU regressions remain documented. No-regression acceptance fails. Array-root parsing may defer materialization; stringify inputs are fully materialized. RSS is whole-process memory. Full results and rejected experiments are in benchmarks/json_performance/DATA_RECORDS.md. No version bump.
