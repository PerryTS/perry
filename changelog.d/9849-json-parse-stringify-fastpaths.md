Accelerate JSON parse and stringify with bounded scanners, vectorized Unicode and escape counts, exact number formatting, bounded shape metadata, native tape ownership transfer, exact short-array storage and direct final output. A native-only walker now reuses per-call escaped keys for eligible nested ordinary records; unsupported graphs return to the rooted general serializer before callbacks. Inputs remain rooted and are rederived after collection. No version bump or input/output memoization.

Recognize explicitly retained old array-growth aliases in evacuation diagnostics while rejecting temporary evacuation pointers and stale metadata keys. GC collection policy, thresholds, allocation hooks and parse-boundary implementations remain unchanged from 0.5.1520; verify.rs and Verify-mode roots.rs contain the documented diagnostic change.

Measured source 27eeccf79e passes 202 JSON runtime tests and 42 full compiled Node comparisons. The extended fixture preserves a pre-existing inherited Object.prototype.toJSON mismatch; 44 moving-GC runs match Node in 42 cases and the reference in two extended cases. All 77 source hashes and qualified full/994-trial paired matrices are verified. Heterogeneous stringify CPU falls 54.25% versus growth-alias, with peak RSS up 0.453125 MiB; other CPU/RSS regressions and semantic gaps remain. All-row/no-regression acceptance is false at 12/38 CPU, 65/74 peak-RSS and 33/36 retained-RSS targets. See benchmarks/json_performance/results/nested-records/README.md and DATA_RECORDS.md.
