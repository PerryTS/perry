Speed up JSON parsing and stringify with bounded NEON/SSE2/word scanning, direct Unicode escape emission, strict numeric scanning in the tape builder, a duplicate-key index for wide objects, and bounded retained parse-shape metadata. Preserve syntax validation, duplicate-key order and the existing GC policy and parse-boundary hooks from v0.5.1520.

Use stack-buffer ECMAScript number formatting, inline short-string results and guarded compact decimals. Serialize qualifying primitive arrays without recursive traversal or temporary managed roots. Build qualifying strings and small plain objects directly into their final output with one rooted input and a bounded stack plan. The numeric emitter matches Node and Bun on 1,730,283 exact doubles with zero temporary allocations in its leaf harness.

The latest quiet M1 comparison retains the earlier stringify gains: numeric-array stringify takes 1.61 ms versus Node's 1.98 ms and Bun's 2.95 ms. Paired repeats of the new tape scanner reduce numeric-array parse CPU by 6.97% and 1–8 MB record-array parse by 3.26–3.53% against the preceding direct-output candidate. The tape scanner creates no managed allocation or collection point.

Performance acceptance remains open: 9/38 CPU, 58/74 peak RSS and 28/36 retained RSS medians meet the better engine. Repeats still show numeric parse +9.76%, heterogeneous parse +2.78% and escaped stringify +2.41% against the Unicode checkpoint. Tiny-string parse is 1% slower than the preceding candidate, and several paired peak-RSS medians increase by 0.1–0.3 MiB. These causes remain unresolved.

Validation passes 121 runtime tests selected by the JSON filter, 29 compiled fixtures matching Node, 323,211 tape-builder differential inputs, all 152 benchmark output checks, and eight moving-GC stress subjects with two seeds each. The full quiet run includes 456 timing and 432 memory trials, followed by 182 paired timing repeats. Full results, limitations and provenance are in `benchmarks/json_performance/TAPE_SCANNING.md`.
