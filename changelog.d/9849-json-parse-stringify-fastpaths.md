Accelerate JSON parsing and stringify with bounded byte scanners, vectorized Unicode handling, wide-object duplicate-key indexing, bounded parse-shape retention, ECMAScript number formatting, inline scalar results, primitive-array emission and direct final output for qualifying strings and small objects.

Inline scalar parses decode before the existing pending-collection hook and preserve oversized key-cache cleanup. Ordinary allocating parses retain their rooted suppression and parse-boundary scheduling flow. A guarded record emitter validates primitive fields and dense primitive arrays before traversing them without managed allocation, intermediate GC handles or callbacks.

Fix two hazards found during validation: later record descriptors now decline shape-template emission so getters and enumerability are honored; small-object output roots its input before the allocating first prototype lookup. A moving-GC regression test crashes under the old root placement and passes with the correction, with actual input relocation asserted. The record emitter declines the cold prototype lookup until it can use the non-allocating cached path.

All 133 JSON-filter runtime tests, 31 compiled Node comparisons and 20 moving-GC stress runs pass. Qualified record-release performance measurements remain pending. The preceding corrected scalar build passes 30 compiled comparisons and 18 moving-GC stress runs; both of its full performance runs are unqualified because foreign benchmarks overlap them. Initial scalar measurements and source snapshots remain available for reproducibility, with their cache-cleanup issue and paired small-object regressions documented.

CPU/RSS parity and no-regression acceptance remain open. Prior qualified tape-scanner results and unresolved regressions are preserved. GC production policy and gc_bump_malloc_trigger remain unchanged from v0.5.1520; parse_api.rs deliberately changes the scalar boundary flow. Current work and validation limits are in `benchmarks/json_performance/DATA_RECORDS.md`.
