Accelerate JSON parsing and stringify with bounded byte scanners, vectorized Unicode handling, wide-object duplicate-key indexing, bounded parse-shape retention, ECMAScript number formatting, inline scalar results, primitive-array emission and direct final output for qualifying strings and small objects.

Inline scalar parses decode before the existing pending-collection hook and preserve oversized key-cache cleanup. Allocating parses retain their rooted suppression and parse-boundary scheduling flow. A guarded record emitter validates primitive fields and dense primitive arrays before emission without managed allocation, intermediate handles or callbacks. Array emission reuses that validation. Empty-object stringify returns inline before field planning and temporary rooting when the prototype probe cannot allocate; cold lookup retains the rooted general fallback.

Fix two correctness hazards: later record descriptors now decline shape-template emission so getters and enumerability are honored; small-object output roots its input before the allocating first prototype lookup. Forced-moving tests explicitly assert relocation of both empty and nonempty inputs.

The preceding empty-object release passes 135 JSON-filter runtime tests, 32 compiled Node comparisons and 22 moving-GC stress runs. Its qualified full matrix contains 152 output verifications, 456 timing trials and 432 memory trials with 32 clean external observations. A separate 210-trial paired check passes with 13 clean observations. Against the preceding record release, paired stringify CPU falls 11.08% for empty objects, 4.79% for 16 KB record arrays, and 4.00%/3.53%/1.27% for 1/8/20 MB record objects. These gains follow the preceding record emitter's 18.7%/17.3%/7.5% gains at 1/8/20 MB versus the tape checkpoint.

CPU/RSS parity and no-regression acceptance remain open: 11/38 CPU, 58/74 peak RSS and 28/36 retained RSS targets meet the better Node/Bun median. Paired escaped parsing is 0.65% slower and 1 MB object-root parsing 0.31% slower than the preceding record release. Older numeric and small-object parse regressions remain unresolved. The apparent new numeric slowdown in the full matrix does not reproduce in the paired check. Array-root parsing can defer materialization; stringify starts fully materialized. CPU includes normal GC; RSS is whole-process memory.

A subsequent ARM mask-search experiment passed correctness but regressed escaped parsing 50.63% and record-array parsing 5.89–9.51% in a qualified full matrix. It was reverted. Its reproducible source patch and complete losing results remain available. Historical unqualified runs are labeled explicitly and do not count toward acceptance.

GC production policy and gc_bump_malloc_trigger remain unchanged from v0.5.1520 (454daac4f); parse_api.rs deliberately changes the scalar boundary flow. GC-directory changes are tests only. No version bump is included. Current implementation, all rows and validation limits are in `benchmarks/json_performance/DATA_RECORDS.md`.

Primitive inline objects now reuse the object walker's preflight and key order to borrow keys and values once during emission. This build passes 138 JSON runtime tests, 33 compiled Node comparisons and 24 moving-GC runs, plus a qualified full matrix and 350 paired trials. Paired wide-object stringify CPU falls 26.28% (2.475 ms to 1.825 ms) with peak RSS near 71 MiB. The target inventory remains 11/38 CPU, 58/74 peak RSS and 28/36 retained RSS. Paired tiny-object parse is 2.21% slower and small-record stringify 2.15% slower; scalar and wide-object parse regressions also remain. The escaped-stringify slowdown does not reproduce. This remains a development checkpoint with no-regression acceptance open.

Depth preflight now skips inputs too short to exceed the nesting limit, and wide primitive fields are checked through one direct borrow. This release passes 139 JSON runtime tests, 33 compiled Node comparisons, 24 moving-GC runs, a qualified full CPU/RSS matrix and 546 paired trials. Against primitive-object, paired small-record/tiny-object parsing improves 9.99%/4.57% and wide-object stringify improves another 7.15%. Large record parsing regresses 1.78–2.80% and heterogeneous stringify 1.60%; no-regression acceptance remains open. The inventory is still 11/38 CPU, 58/74 peak RSS and 28/36 retained RSS. Sampling confirms small-record stringify reaches the shape path before the generic preflight and identifies per-call template construction as a remaining cost.

Root ordinary JSON parse inputs before pending collection and rederive tape
input after collection. Forced movement tests reproduce and fix valid-input
SyntaxErrors. Outline scalar decoding from container calls and restore nested
one-field primitive emission. Qualified paired heterogeneous stringify improves
4.85%, while scalar/escaped-string CPU and some lazy-array peak RSS regressions
remain documented; all-row performance acceptance is still open.
