Fix unbounded parsed-function retention in runtime `new Function` after the source cache reaches its 4,096-entry or 32 MiB limit, and when parse caching is disabled. The GC now drops registry ASTs once their closures are dead, while preserving source-cache entries and active calls/construction. Weak closure owners follow relocation and respect old-generation liveness during minor collections. Active AST pins are released through interpreter root-stack savepoints even when throws skip Rust destructors. Nested-function address caches are invalidated before AST reclamation, preserving escaped nested closures without stale address reuse.

Validation: interpreter unit coverage and real full/copying-minor GC tests cover cache overflow, disabled caching, active AST ownership, escaped nested closures, relocation, exception unwinding, nested-cache rebuilding, and old-generation owners.
