Unlanded R7 experiment: outline the general JSON.stringify fallback while preserving the public ABI, callback order, and fallback body. The linked entry frame falls from 272 to 160 bytes, but short-string, zero-spacing, and key-list controls regress against R5. This candidate is rejected for landing; full CPU/RSS rows and validation are recorded in benchmarks/json_performance/STRINGIFY_ENTRY.md. GC policy and object representation are unchanged.
