The historical-entry-before-callback output pair is the initial 39-line R5 check before the callback-only allocation section was added. It is retained as history and is not final validation. Final frozen-source R5 entry validation is r5-entry-proof.json with r5-entry-normal and r5-entry-scheduled output pairs, each matching the final 40-line Node output. The two failed unit logs and source hashes likewise record fixture development; unit.log and unit-source.json are the final passing source.
