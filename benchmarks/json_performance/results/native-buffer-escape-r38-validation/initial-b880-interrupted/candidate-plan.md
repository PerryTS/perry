R38 reuses the existing UTF8-validated exact escaped-output plan to write large strings into the general serializer native buffer. Small escaped strings retain the old loop. Invalid UTF8/WTF8 declines without mutation and retains normalization fallback. Plan UTF16 units are unused in this native buffer; managed output metadata is computed by its existing constructor. Preserve geometric buffer growth so closing punctuation does not trigger immediate doubling. No GC policy/admission/root-order change. New large-key/value allocating-replacer fixture requires fresh both-arm outputs and native/shadow checks. All earlier parse changes remain controlled by fresh screens.
