R38 reuses the checked expansion plan/bounded writer for large strings in general stringify buffers and uses bulk ASCII detection in the managed builder. First validate all105existing candidate executions and fresh large-emitter both-arm behavior/GC/static checks (20 executions per arm). Preserve all reference failures and any checker findings; do not suppress or change allowlists. Measure rotating15 and Korean6, then options7 plus large-options6; broaden to full50, changing and retained only if qualified. Recheck the small fresh parse trend independently. Keep GC policy, source admission and cache limits unchanged. R26 is the reference, not current main.
