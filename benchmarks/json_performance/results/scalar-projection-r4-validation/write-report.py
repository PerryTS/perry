from pathlib import Path
import json
w=Path(__file__).resolve().parent
b=w.parents[1]
a=json.loads((w/'access-analysis.json').read_text());f=json.loads((w/'focus-analysis.json').read_text());x=json.loads((w/'tiny-crossover-analysis.json').read_text())
sha=json.loads((w/'provenance.json').read_text())['source_commit']
lines=['# Scalar projection R4: branch-likelihood experiment','','**Rejected for landing.** The hint restores ordinary-array dispatch order and','removes the 20 MiB repeated-read regression, but 20 MiB mixed-field access regresses','4.49% versus main with separated sample ranges. Smaller-array access medians are','also slower than R3’s earlier window. The remaining stringify differences are','unresolved. This does not meet the all-rows/no-regressions objective.','',f'Measured implementation: `{sha}` on `codex/json-scalar-projection-r4`.','Reference main: `53df2c671fff33b1f8f624432372c2401db8b1d0`, version 0.5.1530.','[R3 report](SCALAR_MATERIALIZED.md) records the preceding implementation and the','actual-main reproduction of two inherited native-root findings. No R4 PR is open.','','## Change and generated-code result','','R4 adds `llvm.expect.i1(is_lazy, false)` at the optional JSON brand test and declares','the intrinsic. It supplies branch-likelihood information while retaining the actual','condition and both existing outcomes. LLVM lowers this intrinsic into branch weight','metadata. [LLVM source](https://llvm.org/doxygen/LowerExpectIntrinsic_8cpp_source.html).','','The installed LLVM 22.1.4 backend emitted Array → Object → JSON comparisons in the','repeat loop. R3 emitted JSON → Object → Array; main emitted Object → Array. Thus the','hint changed the actual machine dispatch, not just its source spelling.','[Machine-code evidence](results/scalar-projection-r4-validation/machine-order.json).','','No runtime source, GC policy, guard, memo layout or output representation changed','relative to R3. Compiler, runtime-static and stdlib-static were rebuilt together.','The static archive hashes differ from R3; all measurements use their own frozen','artifacts and provenance, not an assumption of byte identity.','','## Measurements','','Quiet M1 Mac mini, 8 GiB RAM; Node 26.5.1 and Bun 1.3.14. The candidate uses seven','fresh-process trials per engine and row, interleaved with identical work counts.','CPU is user + system time per operation. Peak RSS includes process startup/input','construction. Every full-work checksum, sample vector and median was verified.','']
for kind,data in [('access',a),('focus',f)]:
 v=data['quiet_window'];lines += [f"- {kind.capitalize()}: {v['started_utc']}–{v['finished_utc']}; {data['verified_timing_rows']} timed trials, {data['verified_oracles']} verification outputs; load {v['load_before'][0]:.3f} → {v['load_after'][0]:.3f}, no competing workloads. [Window](results/quiet-scalar-projection-r4-{kind}/window.json), [raw timings](results/quiet-scalar-projection-r4-{kind}/timing.jsonl)."]
lines += ['','### Parse, scan and stringify CPU (µs per operation)','','| Fixture | Operation | Main | R4 | Node | Bun | R4 vs main | Ranges vs main |','| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |']
for r in f['rows']:
 c=r['cpu_us'];lines += [f"| {r['fixture']} | {r['operation']} | {c['baseline']:.6f} | {c['perry']:.6f} | {c['node']:.6f} | {c['bun']:.6f} | {r['cpu_delta_percent']:+.2f}% | {'separated' if r['cpu_separated_vs_main'] else 'overlap'} |"]
lines += ['','The three smaller scan rows retain roughly 50–62% CPU reductions versus main.','All their sample ranges beat both Node and Bun. The 20 MiB scan and round-trip','overlap main but still trail Bun; round-trip takes 1.53 times Bun’s CPU.','Small-record stringify (+0.76%) and long-string stringify (+5.86%) have overlapping','ranges; neither is accepted as a no-regression result or a stringify improvement.','','### Parse-once access CPU (µs per iteration)','','One million iterations per process, zero warmup, parse outside the timer.','','| Fixture | Pattern | Main | R4 | Node | Bun | R4 vs main | Ranges vs main |','| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |']
for r in a['rows']:
 c=r['cpu_us'];lines += [f"| {r['fixture'].replace('records_array_','')} | {r['operation']} | {c['baseline']:.6f} | {c['perry']:.6f} | {c['node']:.6f} | {c['bun']:.6f} | {r['cpu_delta_percent']:+.2f}% | {'separated' if r['cpu_separated_vs_main'] else 'overlap'} |"]
lines += ['','20 MiB repeated reads now overlap main (+0.04%), addressing the specific R3','regression (+5.59%). Mixed fields move in the other direction: +4.49% versus main,','with separated ranges. All twelve access CPU rows still trail both Node and Bun.','Every smaller access median is slower than R3’s earlier window; that cross-window','observation is not a simultaneous R3/R4 comparison, but it rules out claiming an','unqualified improvement across the measured patterns.','','### Peak RSS (MiB)','','| Fixture | Operation/pattern | Main | R4 | Node | Bun |','| --- | --- | ---: | ---: | ---: | ---: |']
for r in f['rows']+a['rows']:
 m=r['peak_rss_mib'];op=r['operation']+(' (reuse)' if r['kind']=='access' else '')
 lines += [f"| {r['fixture']} | {op} | {m['baseline']:.2f} | {m['perry']:.2f} | {m['node']:.2f} | {m['bun']:.2f} |"]
lines += ['','Scan peak RSS remains about 67 MiB at 1 MiB and 109 MiB at 8 MiB, versus main’s','414 and 530 MiB. The 16 KiB scan still slightly exceeds Node. 20 MiB scan RSS','remains 692 MiB versus Bun’s 323 MiB. Small absolute increases in other rows remain','visible in the table. Retained-RAM and rotating-input matrices were not rerun,','because the targeted CPU comparison already rejects this candidate.','','## Tiny-stringify crossover','','This separate diagnostic investigates the R3 regression. It times **R3**, not R4,','against main and a crossover consisting of main’s existing worker object linked','with the frozen R3 runtime. The existing imported function ABI and eager','Object/Array layouts remain compatible; the binary is used only for this diagnostic.','Normal, scheduled-moving and full-GC outputs and full checksums match Node. The','scheduled diagnostic protects 106 retired sets and moves 12,720 objects.','']
v=x['window'];lines += [f"The qualified window is {v['started_utc']}–{v['finished_utc']}, load {v['load_before'][0]:.3f} → {v['load_after'][0]:.3f}, no competing workloads. Nine trials per engine (45 total), ten million iterations and 5,000 warmup operations each; six verification outputs. [Window](results/quiet-scalar-projection-r4-tiny-crossover/window.json), [engine mapping](results/quiet-scalar-projection-r4-tiny-crossover/diagnostic.json), [provenance](results/quiet-scalar-projection-r4-tiny-crossover/hybrid-provenance.json).",'','| Engine/build | Median CPU µs/op | Sample range | Peak RSS MiB |','| --- | ---: | --- | ---: |']
for e,label in [('baseline','Main compiler + main runtime'),('prior','Main compiler + R3 runtime'),('perry','R3 compiler + R3 runtime'),('node','Node'),('bun','Bun')]:
 r=x['arms'][e];c=r['cpu_samples_us'];lines += [f"| {label} | {r['cpu_us']:.7f} | {min(c):.7f}–{max(c):.7f} | {r['peak_rss_mib']:.2f} |"]
lines += ['','R3’s median is +0.73% versus main; the crossover is +0.41% versus main; R3 is','+0.32% versus the crossover. **All three ranges overlap.** This does not isolate','one cause, establish a runtime/codegen split, or clear the stringify regression.','All three Perry binary hashes and all 45 timed checksums were independently verified.','','## Validation','','- Exact three-package release build completed in 6m06s; frozen compiler and both archive hashes still match after unit tests.','- 301 runtime JSON and 10 codegen JSON tests pass; 13 index-get, 25 property-get and 15 GC-effect selections pass.','- All 31 fixture lines match Node in nine auto/tape/direct × normal/scheduled/full-GC modes. Each scheduled arm protects 776 retired sets; auto/tape moves 33,614 objects and direct moves 88,384.','- Nine mutation and twelve access controls pass. Descriptor/getter, forwarding, prototype, identity, growth/shrink and coercion checks remain in the fixture.','- Native benchmark workers: 18 functions, 582 statepoints, 1,239 relocates, zero hazards. Full native fixture: the same two inherited unsuppressed findings reproduced on actual main during R3; both full shadow checks pass.','- Every emitted scalar probe has a matching expectation intrinsic: 54 fixture, 12 access-worker and 4 original-worker sites under both root lowerings. Neither changes the noncollecting probe contract.','- Script gates: 73/74 pass; public benchmark freshness still fails. Compile lint tier skipped. No complete CI or JSON-conformance pass is claimed.','','[Validation manifest](results/scalar-projection-r4-validation/manifest.json),','[build provenance](results/scalar-projection-r4-validation/build-provenance.json),','[GC witness](results/scalar-projection-r4-validation/gc-witness.json),','[completion record](results/scalar-projection-r4-validation/completion.json).','','## Next investigation','','Reordering the per-access branches produced a tradeoff. The existing','`element_shape_loop` machinery instead validates an array once before a call-free','loop, keeping necessary residual checks. Its matcher requires a declared class','candidate, raw-f64 fields and the counter itself as the index. The runtime proof','explicitly rejects class ID zero. Consequently these untyped JSON records and','constant/modulo index patterns do not receive that optimization.','','Investigate a distinct, validated numeric-data-record loop contract. A bounded','first slice is a loop-invariant indexed numeric read: establish a side-effect-free','numeric value once, then use it in a call-free reduction. Zero-trip loops must not','touch a potentially throwing receiver, and failed probes must never invoke a getter','before falling back. Varying-index and mixed-field loops need their own precise','guard and side-exit proof. Do not weaken the existing typed-class invariant or','change collector policy. [Detailed constraints and code pointers](results/scalar-projection-r4-validation/next-loop.md).','','The overall objective still includes parse, stringify, all access controls,','changing inputs, peak RSS and retained RAM. The full original matrix was not rerun','for this rejected candidate. Large-input round-trip/RSS and inherited JSON','canonicalization/conformance limitations remain separate outstanding work.']
(b/'SCALAR_BRANCH_HINT.md').write_text('\n'.join(lines)+'\n')
