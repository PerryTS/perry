The exact R26 reference was freshly compiled against the committed emitter regression fixture at its real test-files path. All 20 executions were retained: 14 pass full Node output; six fail. In both native and shadow modes, auto/direct scheduled runs exit 1 with corrupted retained callback output. The auto depth-64 diagnostic exits with SIGBUS (-10). Forced tape passes, including positive copying/protection. Normal and non-generational controls pass. This is a runtime pointer-lifetime finding, not a native-only generated-root failure: all three fresh native/shadow static checker variants pass.
