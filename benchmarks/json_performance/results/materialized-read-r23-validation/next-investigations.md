# Follow-ups grounded in main1a9 diagnostics

Symbolized exact-main-runtime diagnostic (main-lazy-debug) reproduces 180 canonical records with spacer zero: EXC_BAD_ACCESS in stringify_scalars::write_escaped_bytes_from, called by js_json_stringify_full. x21 length is 0x4c5a5841, the lazy-array magic. See main-lazy-zero-symbolized.log and pinned debug provenance. No timing claims for the relinked diagnostic.

stringify_value and stringify_value_depth match GC_TYPE_ARRAY but omit GC_TYPE_LAZY_ARRAY; unknown fallback can interpret a lazy header as StringHeader. Plan a separate correctness fix routing known lazy arrays through existing materializing array serializer. Audit function/key replacer dispatch too (both omit lazy tag); preserve callback root/identity semantics and positive moving witnesses. Do not normalize zero to undefined or widen source-copy admission to mask this. Raw-source whitespace/duplicate-key mismatches remain separate, preserved issues.

Read residual candidate after R23: js_packed_arraylike_index_get proves GC_TYPE_ARRAY|GC_TYPE_LAZY_ARRAY then calls generic js_array_get_f64 which reclassifies. A direct guarded dispatch could use the existing proof (lazy header live/stable, dense ordinary header unforwarded/no descriptors/in bounds/nonhole). Keep forwarded/sparse/hole/descriptor/exotic receivers on original dispatcher. Evidence needed: qualified R23 profile and general-array regression matrix, since this helper is broader than JSON. Do not infer object-shape thrashing; DirectParser seeds shape cache.

Additional stringify coverage to preserve semantics: a lazy parsed array nested inside an ordinary object or ordinary array reaches stringify_value_depth and needs the same tag dispatch; no root raw-source fast path can rescue that shape. Include zero/true/empty-string/negative/NaN spacer (actual inert formatting arguments preserved), identity callback and key whitelist, both root and nested lazy values, duplicate keys/whitespace, mutation, GC-positive runs. Distinguish no-replacer compact array dispatch fix from broader callback rooting. Explicit GC_TYPE_LAZY_ARRAY arm should route only known live lazy headers to the existing safe materializing walk, without structural heuristics or raw-source widening.

New 18-case main-only lazy-dispatch probe completed (lazy-dispatch-main.json): root/object-wrapper/array-wrapper times plain/zero/true/pretty/callback/keys. 14 SIGSEGV, 4 Node matches (root plain plus all three pretty shapes). This is diagnosis, not R23 candidate validation or performance evidence. Exact source/binary/compiler/runtime hashes recorded. Compact stringify_array_depth has a THIRD per-element GC tag match that also omits lazy arrays; must fix in addition to stringify_value and stringify_value_depth.

Callback rooting caution: dispatch_pointer_with_replacer currently resolves ordinary arrays before passing its raw replacer onward. Adding lazy to that same arm introduces allocation in clean_arr_ptr (full materialization), which can move the replacer. Root and refresh the replacer for that new cold lazy-materialization path before delegating. Do not assume caller roots rewrite the callee's raw locals. Pretty array currently holds raw elements across recursive calls, so new moving-GC nested-lazy coverage may expose a baseline root-custody gap; test/repair explicitly rather than just matching no-stress output. Compact array/object loops already root owners and reload slots after recursive calls. Keep callback and whole-tree forced moving witnesses substantive.

Both stringify_array_pretty and stringify_array_with_array_replacer cache raw element bases across recursion; introduce/validate scoped owner reloads if the lazy-child allocation probes exercise that bug. Replacer array walk already roots arr/replacer, but currently snapshots elem/replacer before js_string_from_bytes for an index key and before apply_to_json_keyed; scheduled GC tests must include those windows. Do not mistake an outer live root for a refreshed raw local. Existing ordinary compact loops demonstrate the scoped reload pattern to preserve.

Further read hypothesis, to profile after R23: resolve_materialized_array receives an explicitly live ordinary-array GC edge but routes it through generic clean_arr_ptr and ownership classification every read. A carefully specified helper for a trusted ordinary-array edge could check forwarding and descriptor/bounds directly, retaining the full resolver for forwarding; no GC policy change or cache knobs. Need prove all callers' edge-liveness invariant and preserve growth/actual copied-GC tests; do not weaken arbitrary user-pointer classification. Alternative/broader packed dispatcher proof reuse described above. The fields worker performs THREE rows[index] reads per iteration (id/name.length/active); do not call its timing a single property-read latency.

Packed-index disassembly also exposes a lower-risk arithmetic target: canonical_u32_index first checks JSValue::is_number and then finite/nonnegative/integral/u32-1 bounds; the combined emitted validation is long. is_number itself only excludes the positive qNaN tag band (value/jsvalue.rs:67), so the expanded finite/fraction validation must not be attributed to is_number alone. A bounds-first proof can exclude all NaN tags and raw-pointer denormals without the generic number predicate. Preserve explicit INT32 decoding; accept exact +/-0 by bits; otherwise require >=1, <=u32::MAX-1 and integral. The >=1 gate explicitly excludes denormals even if FP flush-to-zero changes, and the upper bound excludes infinity. This is only a candidate design, NOT implemented or timed. Test NaN tag bands, INT32 negatives/bounds, raw pointer bit patterns, subnormals, -0 and 2^32-1 property fallback plus broader array tests before using it. The current packed helper's large stack frame also reflects subclass cold branches; outlining may matter, but measure.

An even smaller canonical-index candidate is a saturating u32 cast plus exact float round-trip, after the existing INT32-tag branch: let i=value as u32; accept iff i!=u32::MAX && f64::from(i)==value. Rust float-to-int casts saturate, and the exact float equality rejects NaNs (all non-number tags), infinity, negative nonzero values, fractions and out-of-range numbers; +/-0 remain index0. All u32 values are exactly representable as f64. This should lower to FCVTZU/UCVTF/FCMP plus the max-index guard on ARM64. Not implemented/timed; preserve old behavior with broad bit-pattern and actual dynamic-index tests before any performance claim. Prefer measured code to guessed instruction costs.
