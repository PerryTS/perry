R37 final source599b: initial77ea unit/lint attempt retained, no initial production build. Final code separates the large-token length proof, outlines both constructors, handles valid ED continuation with vector masks, and scans word spans only on ordinary bytes. Require all105candidate outputs/28IR/sixworker objects; then rotating15 and Korean6, options7/full50/large6/changing2/retained/access if qualified. No prior-source gains presumed. GC suppression/root order/admission/policy unchanged.
