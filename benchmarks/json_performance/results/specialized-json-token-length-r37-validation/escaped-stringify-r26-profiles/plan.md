Two bounded remote diagnostics from exact frozen R26 options worker: escaped_1m pretty/callback, 2000 calls and8warmup,1s sampler after0.5s,20s/2GiB watchdog. Full pinned Node loop/output/checksum oracle. No sampled CPU/RSS performance claim. Archive every terminal window FIRST before any subsequent remote action. Targets the known roughly3xBun escaped option gap, while independent parse candidate compiles locally.
