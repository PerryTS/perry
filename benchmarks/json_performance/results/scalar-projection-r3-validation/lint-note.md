The script lint run uses SKIP_COMPILE_GATES=1. An initial invocation with --help unexpectedly started a full run because the script has no help option; it was stopped. The overlapping harness fixture cleanup made the logged peer-fallback gate fail with a missing 13_factorial fixture. The standalone peer-fallback follow-up log records the rerun after that overlap ended. The full script run and any real remaining failures are retained; no gate is suppressed.
