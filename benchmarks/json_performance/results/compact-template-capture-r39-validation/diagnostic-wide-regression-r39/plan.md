Both independent R39 windows confirm the selected regressions. The first reference diagnostic hit its 2 GiB RSS cap. A retry-preparation syntax error then invoked the original controller again; its existing result directory refused execution. Original profile data is intact, but its first window envelope was overwritten and is preserved only as a prior tool observation. Both failed attempts are excluded. The corrected retry uses shorter parse loops in both arms, the same 2 GiB and 20-second watchdogs, one-second sampler at 1 ms and 500-workload-sample floor. Pinned Node must match every complete diagnostic loop. Sampling is attribution only.
