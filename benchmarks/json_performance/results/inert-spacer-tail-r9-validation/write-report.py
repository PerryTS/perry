from pathlib import Path
import json
w=Path(__file__).resolve().parent;bench=w.parents[1]
a=json.loads((w/'options-analysis.json').read_text());p=json.loads((w/'provenance.json').read_text());phase=a['phases']['options'];win=phase['window']
lines=['# JSON stringify inert-spacer helper (R9)','','**Rejected for landing:** ordinary small-record stringify slows by 2.82% with separated sample ranges. Literal and computed zero spacing are about 9.3× faster than main; key-list stringify improves 1.92%. Those gains do not satisfy the requested no-regression bar. The remaining matrix was not run after the first seven controls rejected this version.','',f"Source `{p['source_commit']}` on `codex/json-inert-spacer-tail-r9`, based on main `{p['base_commit']}` (0.5.1531). R3/R5/R6/R7/R8 production changes are excluded. The exact fresh-main build from R8 is reused by verified hashes, while its workers, behavioral fixtures and static IR are recompiled at the R9 input paths. This is not a second rebuild of main or a claim to measure any newer main revision.",'','The candidate leaves the original Rust compact admission block intact, then calls a separate `#[inline(never)]` helper for newly inert spacers. The helper admits only primitive true and either sign of numeric zero, tries the four existing bounded writers, and retains the original arguments on fallback. Lazy-source admission, object representation, callback order, and GC policy are unchanged.','','## Measurement','','Four engines on the M1 Mac mini (8 GiB), seven interleaved fresh-process repetitions, Node 26.5.1 and Bun 1.3.14. Plain/literal-zero/computed-zero run two million iterations; pretty/key-list one million; callback 200,000; the 16 KiB pretty array 1,000. Warmup is 5,000 for small records and eight for the array. Inputs are eagerly parsed before timing.','',f"Quiet window: {win['started_utc']} to {win['finished_utc']}, one-minute load {win['load_before'][0]:.3f} to {win['load_after'][0]:.3f}. The quiet gate passes, with no detected competing workload at either boundary. The terminal window was archived before any subsequent remote operation.",'','CPU is microseconds per complete stringify operation; negative deltas are faster. All samples and outliers are retained. “Separated” means observed sample ranges do not overlap; it is not a confidence interval or proof of causality.','','| Workload | Main | R9 | Node | Bun | R9 vs main | Ranges |','|---|---:|---:|---:|---:|---:|---|']
for c in phase['cases']:
 e=c['engines'];values=[f"{e[n]['cpu_us']:.6f}" for n in ['baseline','perry','node','bun']];note='separated slowdown' if c['separated_regression'] else 'separated gain' if c['separated_improvement'] else 'overlap';lines += ['| '+c['fixture']+' / '+c['operation']+' | '+' | '.join(values)+f" | {c['delta_pct']:+.2f}% | {note} |"]
lines += ['','Peak RSS is MiB for the whole process, including input, runtime, output, and allocator storage. It is not retained heap.','','| Workload | Main | R9 | Node | Bun |','|---|---:|---:|---:|---:|']
for c in phase['cases']:
 e=c['engines'];values=[f"{e[n]['peak_rss_mib']:.2f}" for n in ['baseline','perry','node','bun']];lines += ['| '+c['fixture']+' / '+c['operation']+' | '+' | '.join(values)+' |']
slug='quiet-'+w.name+'-options';v='results/inert-spacer-tail-r9-validation/'
lines += ['',f'[All 196 timed samples](results/{slug}/timing.jsonl), [complete-output verification](results/{slug}/verify.jsonl), [all CPU/RSS vectors and medians](results/{slug}/summary.json), [quiet window](results/{slug}/window.json).','','## Machine-code finding','','The original Rust block does not retain its original machine code. Adding a second production caller makes LLVM emit calls to `try_primitive`, `stringify_record_output::try_object`, and `stringify_flat::try_object` from the public entry; main inlines all three. The heap-string attempt remains inlined. The public frame grows from 272 to 304 bytes; the accepted helper path has a separate 32-byte frame. The helper uses integer comparisons for true and both zero bit patterns in this build.','','This identifies a concrete change to investigate, without proving that it alone causes the measured slowdown. A bounded follow-up is to strengthen only those three writer annotations from `#[inline]` to `#[inline(always)]`, leaving the new helper out of line. Source search finds only the public entry and the new helper as their production call sites; other references are test-only. That follow-up is not implemented or measured in R9.','',f'[Machine observations and exact disassembly commands]({v}machine-observations.json), [main entry]({v}main-stringify-full.s.gz), [candidate entry]({v}candidate-stringify-full.s.gz), [candidate helper]({v}candidate-inert-helper.s.gz), [follow-up notes]({v}next-inline-investigation.md).','','## Build and validation','','- Candidate built from clean, frozen source with `cargo build --release -p perry -p perry-runtime-static -p perry-stdlib-static`. All three artifact hashes are recorded and modification times are after build start. The recorded 590.88 seconds includes waiting for the broader unit-test build to release Cargo’s build-directory lock.',
'- All four generated worker objects match main byte-for-byte. Each worker is linked with its corresponding verified static runtime archive. All 19 input fixtures are staged and their hashes verified alongside the binaries (60 remote hashes total).',
'- 292 runtime JSON tests pass single-threaded on final source. An earlier narrower `json::` filter also passed 158 tests; both logs and exact commands are retained separately.',
'- Main and candidate each pass 19 Node checks across auto/tape/direct parsing and normal/scheduled/full GC, including callback-only GC activity; each also passes 14 options checks. Every scheduled run asserts positive protected page sets and moved objects. The new spacer fixture records 1,190 protected sets and 44,380/44,455/44,380 moved objects. Callback-only records 16 protected sets and 13,305 moved objects.',
'- Full native IR checking retains nine UNSUPPRESSED findings (eight unrooted global values, one stale allocation value). All match actual main fingerprints. All twelve IR files match main after removing only the first native ModuleID file-path comment; shadow files need no normalization. Both shadow checks pass. Native ordinary workers and callback-only controls have zero findings. This is not an all-clean static result.',
'- Script lint: 73/74 pass, public benchmark evidence freshness fails; compile tier and two CI-only checks are skipped. The Rust file cap passes. Full CI is not claimed.','',f'[Build provenance]({v}build-provenance.json), [reference-main provenance]({v}reference-main.json), [object comparison]({v}worker-object-comparison.json), [candidate behavioral checks]({v}candidate-fixture-validation.json), [options checks]({v}candidate-options-validation.json), [root comparison]({v}root-comparison.json), [lint log]({v}script-lint.log.gz).','','## Known baseline gaps remain visible','','All 24 separately isolated lazy-array cases exactly preserve main outcomes and complete stdout. For 180 records, six zero/true spacing cases crash with SIGSEGV, and plain whitespace/duplicate-key inputs return noncanonical raw JSON. The remaining large cases and all twelve two-record cases match Node. Preserving these failures is not a conformance pass. Normalizing zero to undefined would widen the existing raw-source shortcut and change behavior, so fallback retains the original spacer.','','The fractional-spacing probe also preserves the separately recorded main/Bun versus Node output difference for positive fractions below one. It is excluded from passing Node checks and retained as an explicit baseline limitation.','',f'[Lazy baseline]({v}lazy-main-probes.json), [candidate lazy outcomes]({v}lazy-candidate-probes.json), [fraction baseline]({v}fraction-baseline.json), [candidate fraction check]({v}candidate-fraction.json).','',f'All 196 timed checksums, full output hashes, CPU/RSS sample vectors, medians, recorded source/worker/input hashes, source patch and quiet window are independently verified by [analyze.py --options-only]({v}analyze.py). The full original 38-row parse/stringify matrix, broader consumption/access, rotating, retained-memory, and short-call suites were not rerun on this rejected candidate. Their requirements remain open.','','[R8 results and longer recheck](https://github.com/PerryTS/perry/blob/25b534011257910155c51e5e4362e53b6ddad219/benchmarks/json_performance/INERT_SPACER_R8.md) document the preceding experiment. R9 and R8 use different source paths and some different work counts, so their absolute medians are not an isolated same-window A/B.','']
(bench/'INERT_SPACER_TAIL_R9.md').write_text('\n'.join(lines));(w/'report-draft.md').write_text('\n'.join(lines));print('Wrote all seven R9 CPU/RSS rows and qualification limits.')
