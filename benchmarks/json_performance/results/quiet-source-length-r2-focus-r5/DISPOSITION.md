# R2 diagnostic window

This candidate still has the enlarged shared parser frame and is based on PR
#10032 before the confirmed lazy-surrogate decoder fix. It is preserved as a
quiet diagnostic comparison, not final release acceptance. See
`../../SOURCE_LENGTH.md` and `../source-length-r2-validation/README.md`.
